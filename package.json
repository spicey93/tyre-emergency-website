{
  "name": "tyre-emergency-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "sass": "^1.93.2",
    "wrangler": "^4.87.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.3.0",
    "@astrojs/sitemap": "^3.7.2",
    "@fontsource/rubik": "^5.2.8",
    "astro": "^6.1.9",
    "bootstrap": "^5.3.8",
    "bootstrap-icons": "^1.13.1"
  },
  "overrides": {
    "vite": "^7"
  }
}
